<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jason.blackdoglab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlackDogLab.Default">
        <activity android:name=".ParkActivity"></activity>
        <activity android:name=".StreetActivity" />
        <activity android:name=".DogActivity" />
        <activity android:name=".loginpage.DailyLoginActivity" />
        <activity android:name=".loginpage.FirstLoginActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".MainPage" />
        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|orientation|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>